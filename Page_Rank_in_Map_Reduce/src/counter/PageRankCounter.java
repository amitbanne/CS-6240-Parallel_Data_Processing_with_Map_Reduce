package mr.page_rank.counter;

public enum PageRankCounter {
	PAGE_COUNTER, // CONSTANT for counter to compute total PAGES in dataset
	DANGLING_NODE_SCORE // CONSTANT for counter to compute total DANGLING_NODES scores in one iteration
}
